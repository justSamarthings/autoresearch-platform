# Backend / Supabase (placeholders only — copy to backend/.env and fill in)
DATABASE_URL=postgresql+psycopg://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Optional: used by scripts/register_experiment.py (Colab should set this to a public/tunnel URL)
# AUTORESEARCH_API_URL=http://127.0.0.1:8000

